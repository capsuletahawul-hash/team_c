export const aiService = {
    async askAboutCourses(question: string) {
        // رد تجريبي مؤقت حتى يجهز كود الـ LLM الفعلي
        return `هذا رد تجريبي على سؤالك: "${question}". سيتم ربطه بمزود الذكاء الاصطناعي قريباً.`;
    },
};